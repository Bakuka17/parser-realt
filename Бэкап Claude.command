#!/bin/bash
# Двойной клик → копия наших настроек Claude (~/.claude: делегаты, правила, память) в Яндекс.
# Claude зовёт этот бэкап сам после изменений; кнопка — чтобы Денис мог запустить вручную в любой момент.
bash "$HOME/.claude/backup_to_yandex.sh"
echo
echo "Готово. Можно закрыть окно."
