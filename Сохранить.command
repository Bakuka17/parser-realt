#!/bin/bash
# Двойной клик по этому файлу = сохранить отмеченные объекты (фото + гео-анализ).
# Сначала поставь метку в колонке «Сохранить» в commercial_realty.xlsx, сохрани и закрой файл.
cd "$(dirname "$0")"
./bin/python save_marked.py
echo ""
echo "Готово. Окно можно закрыть."
read -n 1 -s
