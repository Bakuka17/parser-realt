# Проект: парсер коммерческой недвижимости Беларуси

Инструмент для лидогенерации: собирает объявления коммерческой недвижимости из
нескольких источников в один Excel с телефонами/адресами для обзвона.

Окружение: venv в `/Users/denis/realty_env`. Запуск Python: `./bin/python`.
Зависимости: `playwright`, `openpyxl`, `playwright-stealth`, `beautifulsoup4`+`lxml` (HTML-парсинг —
ПРЕДПОЧТИТЕЛЬНЕЕ регэкспов, в т.ч. в брифах ботам), `pypdf`. Системно: `poppler` (чтение PDF),
`tesseract`+`rus.traineddata` (OCR PDF-сканов — опц., только где надо: `auctions_common.pdf_text()`,
движок глобальный `~/.claude/ocr_pdf.py`), `git` (репозиторий инициализирован 01.06, первый коммит
сделан; venv/xlsx/photos в .gitignore).
Платформа: macOS.
**Бэкап проекта: Яндекс.Диск** — `~/Yandex.Disk.localized/realty_backup/` (в Finder показывается
как «Яндекс.Диск»). Синкается ВСЁ: код, все xlsx (включая `saved_realty.xlsx`), папка `photos/`,
CLAUDE.md, `.command`-лаунчер. iCloud-бэкап убран — пользователь предпочёл одну облачную копию
в Яндексе. Константа в коде: `BACKUP_DIR` в `collect_realty.py`.

## Источники и парсеры

| Файл | Источник | Метод | Телефоны |
|---|---|---|---|
| `realty_parser_v8.py` | realt.by | Playwright (карточки + клик «показать контакты») | ~90% |
| `megapolis_parser.py` | megapolis-real.by | urllib, серверный HTML (rItem-секции) | ~90%, прямо в списке |
| `kufar_parser.py` | kufar.by | urllib, JSON из `__NEXT_DATA__`, курсорная пагинация | НЕТ (антибот на деталке — TODO) |
| `gohome_parser.py` | gohome.by | urllib, серверный HTML (`w-object-list-item`); каркас от GLM, фиксы мои | ~100%, прямо в листинге (`tel:`) |
| `byrealty_parser.py` | byrealty.by | urllib, серверный HTML (движок Sitebill); мелкий (~7) | ~100%, в листинге |
| `domovita_parser.py` | domovita.by ⚠ **бел. IP** | urllib, серверный; телефон/коорд на деталке (`data-phone`) | ~100% (на деталке) |
| `edc_parser.py` | edc.sale ⚠ **бел. IP** | urllib, серверный листинг (агрегатор РБ) | НЕТ (телефон за JS `j-c-phones` — TODO, как kufar) |
| `bc_parser.py` | bc.by ⚠ **бел. IP** | urllib, серверный HTML **windows-1251** (аренда госпомещений ГХУ Управделами, 7 областей) | 100% (колл-центр + кураторы областей с ?id=149) |
| `collect_geo.py` | domovita+edc+bc | **оркестратор гео-сбора** (⚠ ЗАПУСК БЕЗ VPN) → дописывает в общий файл | — |
| `geo_probe.py` | domovita+edc | автономный разведзонд гео-сайтов (⚠ БЕЗ VPN) → `geo_probe_out/` | — |
| `merge_sources.py` | — | объединяет xlsx-файлы, опц. `--dedup-cross` | — |
| `collect_realty.py` | realt/megapolis/kufar/**gohome/byrealty** | **главный оркестратор** (под VPN): одна команда → все НЕ-гео источники → один файл | — |
| `save_marked.py` | — | **сохранение избранного**: фото + гео-анализ через OSM | — |
| `doctor.py` | ВСЕ | **health-check** перед сбором (лёгкий urllib-зонд ~5с): кто жив/лёг + страна IP для kufar-телефонов | — |

### Четыре новых источника (25.06.2026) — по просьбе Дениса добавить аренду+продажу коммерции

- **gohome.by** ✅ и **byrealty.by** ✅ — НЕ гео-блокированы, в `collect_realty.py` (gohome дал
  10000 за прогон — упёрся в `--max-pages 100`, есть ещё → для полного `--max-pages 300`).
- **domovita.by** и **edc.sale** — ⚠ **ГЕО-БЛОК** (domovita=nginx 423, edc=TCP-timeout под иностранным
  IP; edc на рос. хостинге Selectel). Собираются ТОЛЬКО с белорусского IP (VPN OFF) через
  `collect_geo.py` — как kufar-телефоны. Структура разведана автономным `geo_probe.py` (запускал Денис
  без VPN). edc-телефон за JS (`j-c-phones`) — открытый TODO (реверс эндпоинта, как был у kufar).
  domovita пока только Минск (`CITIES` в `domovita_parser.py`; регионы — добавить города).
- Запуск гео-сбора (Денис, БЕЗ VPN): `./bin/python collect_geo.py` (дописывает в `commercial_realty.xlsx`).

### bc.by — аренда госпомещений ГХУ Управделами Президента (04.07.2026, наводка Дениса)

⚠ ГЕО-БЛОК + сайт в **windows-1251**. `bc_parser.py`, 3-й источник в `collect_geo.py`.
Листинг `?id=152&search=1&s_area=N` (N=1..7 области, всё одной страницей, пагинации нет):
таблица `tab100p`, заголовок здания = `td[colspan=13]` с `<a ?id=152&bld=…>` (адрес),
лот = `tr.row_data` с 13 td; **9 td = продолжение БЛОКА** (помещения, сдаваемые только
вместе: цены/примечание «БЛОК N кв.м» в rowspan первой строки) — схлопываются в один лид
«Блок помещений». Шум (санузлы/коридоры/тамбуры) отфильтрован (NOISE). Цены BYN/мес и
BYN/м² прямо в листинге. Телефоны: общий колл-центр аренды +375 17 218-18-18 всем +
кураторы областных секторов с `?id=149` (парсятся на лету, у Минска — только общий).
Живой прогон 04.07: **386 лотов по всем 7 областям** (Минск 263, Могилёв 41, Гродно 34,
Гомель 16, Брест 14, Витебск 13, Минская обл. 5). ⚠ Грабли, пойманные на живых данных:
(1) `normalize_url` v8 И `norm_url` auctions_common резали query → ВСЕ лоты bc в один
дедуп-ключ (у bc весь сайт в `?id=`) — теперь ОБА для bc.by query сохраняют; (2) `_deal`
должен быть ИМЕНЕМ ЛИСТА («Аренда») — с «rent» write_excel молча выбрасывает строки.
**Аукционы ГХУ** (`?id=79`, «право аренды на 3 года», обычно 1-2 шт.): анонсы (город/дата/
ссылка) → `collect_auctions()`/`write_auctions()` в bc_parser → `auctions_bc.xlsx`
(пересборка целиком; merge подхватывает по glob) — вызывается внутри BC.collect() при
гео-прогоне. TODO: разбор ЛОТОВ аукционов — деталки `?id=79&news_id=N` сохраняются
фикстурами в `bank_geo_out2/bc/` при живом прогоне (на 04.07 ещё не скачаны — доедут
следующим collect_geo); фото лотов аренды (имена в `onclick=load_foto_rooms(...)`,
путь к файлам не проверен — выяснить живым прогоном).

### Недвижимость за 1 базовую величину (04.07.2026) — `onebv_auctions.py`

Госимущество за 1 БВ (в 2026 = 45 BYN) + аукционы облкомитетов → `auctions_onebv.xlsx`
(пересборка целиком; merge подхватывает по glob). Живой прогон 04.07: **72 лота, из них 54
«за 1 БВ»** (Гомель 49 + Минобл 23, из них 5 за 1 БВ). ⚠ Облсайты «1 БВ» **НЕ гео-блокированы**
(проверено под VPN) — в отличие от банков/kufar, гонять можно в любой сессии без Дениса.
- **Гомель** `gomeloblim.gov.by/realty/?price_base=Y` (bitrix). Грабли: чекбокс = `Y`
  (с `=on` молча «Ничего не найдено»); bitrix прокидывает в href листинга ВЕСЬ query
  (`&PAGEN_1=…`) — регэксп ссылок нестрогий; фото объекта = `<a>` полноразмера внутри
  `.gallery` (голые `img /upload/iblock/` — ЛОГОТИПЫ шапки, не фото!). Телефон = из
  «Балансодержатель и его контакты» (городские `(02333) 7-50-60`), организатор = балансодержатель.
- **Минобл** `minoblim.by/ru/auktsiony` (Joomla K2, все аукционы области, не только 1 БВ).
  Грабли: ссылка деталки в `onclick` кнопки «Подробнее» (не href!); деталка = пары
  `.auction-single--td`; цена бывает СЛОВАМИ «Одна базовая величина» (→ 45 BYN, 5 шт.);
  телефонов в лотах нет → приёмная комитета 8 (017) 500-45-01. K2-фильтр
  (`/ru/component/k2/itemlist/filter?Itemid=104`) работает только с Referer — но не нужен,
  прямой листинг категории проще.
- **Остальное**: ngi.gki.gov.by ВСЁ ЕЩЁ 403 «техобслуживание» (и с бел. IP, зонд 04.07) —
  как оживёт, он лучший (Минск+все области+фильтр 1 БВ); Брест — статьи-извещения по районам
  ПУСТЫЕ (тело не в статике, 2023 г.); Могилёв — лента извещений о торгах (вероятно дубли
  ipmtorgi); Витебск — fondgosim.vitebsk.by мёртв в DNS глобально, раздел на обл-портале
  не найден. Зонд: `probe_1bv2.py` (БЕЗ VPN, дампы в `probe_1bv2/`).

## Фича «Сохранить» (избранные объекты)

Колонка «Сохранить» в основном файле (первая, всегда видна — закреплена вместе с «Тип»).
Пользователь ставит метку (`x`, `+`, `1`) против интересных объектов. Запуск
`./bin/python save_marked.py` или двойной клик по `Сохранить.command`:
- качает фото из колонки «Фото URL» в `photos/{хэш}/` (макс. 3 шт на объект);
- через бесплатный **OSM Overpass** (без API-ключей!) считает:
  - «Активность локации» (POI рядом в 300м: магазины/кафе/услуги/офисы);
  - «До транспорта, м» (ближайшая остановка/метро/станция);
- дописывает в отдельный `saved_realty.xlsx` (лист «Сохранённые») с путями к фото и гео.
- Уже сохранённые (по хэшу) пропускаются — можно запускать многократно.

**Важно для Overpass**: User-Agent должен быть НЕ браузерный (overpass-api.de отдаёт 406
на `Mozilla/...`). Используется `OVERPASS_UA = "realty-tool/1.0"`. Уже зашито.

**Координаты и Фото URL** извлекаются при сборе:
- **kufar** — из `__NEXT_DATA__` (`coordinates`, `images[].path` → `https://rms.kufar.by/v1/gallery/{path}`). Фото ~98%, коорд 100%.
- **megapolis** — из `data-coord` и `data-src` в rItem (lazy-load, **именно `data-src`**, не `src` — `src` это `blank_*.png`-заглушка). Относительные пути дополняются `https://megapolis-real.by/`. Фото 100%, коорд 99% у пересобранных.
- **realt** — фото НЕ извлекаются (см. ниже). Координаты — да, через `page.evaluate` ищу `lat,lng` в innerText/data-атрибутах (~65%). Когда коорд нет, save_marked геокодит адрес через OSM Nominatim.

**Realt-фото — известный TODO**: эксперимент показал, что единственный `a.realt.by/openximg/...jpg`,
видимый в `<img>` сразу после загрузки, — это общая og:image-заглушка (тот же URL на ВСЕХ карточках).
Реальные фото галереи лежат глубже (CSS background-image / lazy через JS-компонент / отдельный API).
Чтобы реально вытащить, нужно либо более долгий wait + точечный селектор галереи,
либо реверс XHR-запросов. **Пока realt-объекты сохраняются без фото.**

Старые `realty_parser_v3..v7.py`, `patch_v8.py` — легаси, перемещены в `legacy/` (31.05), не используются.

## SwiftUI-приложение (`RealtyApp/`)

Нативный GUI-фронтенд для Python-парсера. Папка `~/realty_env/RealtyApp/` —
SPM-проект, открывается через `xed Package.swift`. min macOS 13.
Зависимость: `CoreXLSX` (только чтение). Запись xlsx из Swift не делается —
для «сохранить выбранное» вызывается `save_marked.py --hashes h1,h2,...`,
для удаления из сохранённых — генерируется на лету тонкий Python-помощник.
**Sandbox должен быть отключён** в Xcode (иначе нет доступа ни к Process,
ни к `~/realty_env`). Архитектура MVVM: `AppState` (@MainActor ObservableObject),
`XLSXService` (CoreXLSX), `ProcessRunner` (Process+Pipe со стримингом),
вьюхи `ContentView` / `MainTableView` / `SavedView` / `LogPanel`.

Изменение в `save_marked.py`: добавлен argparse и флаг `--hashes h1,h2,...` —
позволяет SwiftUI-приложению передать выбранные объекты без записи метки в xlsx.

## Веб-дашборд «Консоль обзвона» (`web/`, 11.06.2026)

Веб-фронтенд для обзвона поверх `commercial_realty.xlsx`. Сделан скилом **impeccable**
(`~/.agents/skills/impeccable`, установлен юзером 08.06; product-регистр, OKLCH, системный
шрифт, все состояния, тёмная тема). Запуск: двойной клик `Дашборд.command` (в корне) →
`export_data.py` (xlsx→`web/data.js`) → `web/server.py` (локальный http+API) → браузер.
**Открывать только через сервер (http://localhost:8765), не `file://`** — иначе Safari
блокирует ссылки/кнопки (нужен бэкенд + http-origin для буфера/popup).

- **`export_data.py`** — листы «Продажа»/«Аренда» (шапка=стр.2, данные с 3) → `data.js`
  (`window.LISTINGS`/`META`). Чистит город (`г. Г. Минск`→`Минск`), `0 $`→цена скрыта,
  кладёт `sheet`+`row` (номер строки Excel) для перехода. `data.js` 7+МБ — рендер порциями по 48.
- **`server.py`** — статика + API (только 127.0.0.1, индекс по хэшу из data.js):
  `POST /api/save` (офлайн-копия объявления; фото параллельно, 6 потоков — иначе Safari
  обрывал fetch на минутных сохранениях), `GET /api/saved` (хэши сохранённых — карточки
  сразу зелёные после перезагрузки), `POST /api/reveal` (Excel: активирует книгу/лист,
  выделяет ВСЮ строку A:AE и скроллит к ней; returncode osascript ПРОВЕРЯЕТСЯ — при отказе
  Automation открывает файл + подсказка про Настройки→Автоматизация), `GET /api/ping`
  (+CORS — страница с file:// сама находит сервер и redirect'ится), `POST /api/update` +
  `GET /api/update/status` + `POST /api/update/stop` — **обновление через `_stream()`**, тело POST
  `{target}` (валидируется whitelist'ом). **ТРИ кнопки в шапке (05.07.2026, по просьбе Дениса):
  «Обновить» → target `all`, «Обновить аукционы» → `auctions`, «Обновить банки» → `banks`**
  (кнопки в `.appbar__actions`, вторичные — стиль `btn-update--alt`; при running дизейблятся все три).
  target `all` = полный прогон через `_update_steps()`. **Правило «свежие первыми»
  (05.07.2026): СНАЧАЛА весь сбор свежих объявлений, ПОТОМ доборы телефонов** — квота
  раскрытий kufar дефицитна, пусть уходит на максимально свежую базу. Порядок: `collect_realty.py`
  (realt/megapolis/kufar/**gohome/byrealty**) → **`collect_geo.py` (domovita+edc, БЕЗ VPN)** →
  `collect_auctions.py` (14 площадок, вкл. **onebv**) → `collect_banks.py` → **[доборы:]**
  `kufar_phones.py --limit KUFAR_PHONE_LIMIT` → `belretail_phones.py` → один ре-экспорт.
  (`_update_steps` копит `collect` и `phones` раздельно, возвращает `collect + phones`.)
  Гео-шаг и kufar/belretail-телефоны сами пропустятся, если IP не белорусский (под VPN
  соберут 0, остальное не ломают). Targets `realty`/`geo` остаются CLI-only (в whitelist, без кнопок).
  Один общий JOB/status/stop — два сбора разом нельзя (оба пишут в xlsx). Всё со стримом лога.
  Каждый шаг — отдельный процесс → terminate чистый. После ре-экспорта — шаг прогрева
  фото-кэша (`prefetch_photos.py --limit 2000`, идемпотентно: новые объекты докачиваются).
  **Фото карточек — через локальный кэш `/img?hash=` (22.06.2026), НЕ напрямую с CDN.**
  Корень старой боли «серые карточки / обрезанные фото»: браузерная ПАЧКА из ~48 параллельных
  запросов к `rms.kufar.by` под VPN (иностранный IP) СТОПОРИТСЯ (kufar — 43% базы) → фото
  не догружались (серое) или грузились наполовину (визуально «обрезано»). Диагноз проверен:
  последовательная СЕРВЕРНАЯ загрузка kufar-gallery под тем же VPN = 6/6 ОК (~0.9с/фото);
  стопорит именно браузерный параллельный всплеск. Решение: фронт грузит `<img src="/img?hash=">`
  (см. `photoTag` в app.js), сервер отдаёт фото с диска `web/photos_cache/{хэш}.{jpg,png,webp}`
  (gitignore), при промахе докачивает ПОЛНОразмер (kufar `list_thumbs_2x`→`gallery` 873×690;
  realt/прочее без фото → og:image с деталки) ПОСЛЕДОВАТЕЛЬНО через `FETCH_SEM=3` — пачка
  источник не злит. `.none`-файл = негативный кэш (фото нет, повторно не дёргаем).
  Браузер берёт фото с localhost → мгновенно, целиком, полный размер, НЕ зависит от IP/VPN.
  **Прогрев:** `web/prefetch_photos.py` (или `Прогрев фото.command`) — качает фото ВСЕХ
  объектов в кэш (идемпотентно, resumable, ~3/с под VPN, полный прогон ~1.5ч). Запускать
  под VPN можно (последовательно kufar не банит) или без VPN (быстрее). og:image отдают и
  крупные аукционы: ipmtorgi/cpo ✅ (konfiskat/torgi24/gki/mgcn ❌ — у них og нет).
  **megapolis-фото — кэш-URL протухает (22.06):** сохранённый при сборе `photo`
  (`assets/cache/images/...-600x400.jpeg`) megapolis периодически чистит → 404 → карточка
  «фото недоступно», хотя на самом объявлении фото ЕСТЬ. Фикс: `_photo_urls()` (бывш.
  `_best_photo_url`) отдаёт СПИСОК кандидатов — сперва `photo`, при промахе **og:image деталки**
  (живой оригинал `assets/images/userfiles/...`); `fetch_photo` идёт по списку до первого
  успеха, `.none` ставит лишь когда ВСЕ кандидаты пусты. ⚠ megapolis БАНИТ всплеск (TCP-дроп,
  BrokenPipe) → массовый прогрев залпом наставит ЛОЖНЫХ `.none`. Греть megapolis ТОЛЬКО медленно:
  `prefetch_photos.py --sources megapolis-real.by --workers 1 --delay 4` (новый флаг `--delay`)
  или **`Прогрев megapolis.command`** — лучше БЕЗ VPN (свой IP добрее). Ложные `.none` от бана
  снимаются удалением `{хэш}.none` у megapolis-хэшей и повторным аккуратным прогревом.
  **realt-фото в карточках были «лентой» (22.06, фикс):** `.lead__media img{height:100%}` НЕ
  резолвится от родителя с `aspect-ratio` (без явной высоты браузер трактует как `auto`) → фото
  падало в натуральную высоту, серые поля. Фикс в styles.css: `img{position:absolute;inset:0}`
  (контейнер уже `position:relative`) → заполняет блок при любом соотношении (проверено 0.75–1.78).
  Старый `GET /api/photo?hash=` (realt og:image, `photo_cache.json`) оставлен, но карточки
  теперь идут через `/img`; полный размер для СОХРАНЕНИЯ по-прежнему качает `save`.
  Телефон в сохранёнке: из строки таблицы, иначе regex по тексту объявления
  (`phones_from_text`, ловит `+375…`/`8(0xx)…`, юнит-тесты 6/6); нет → честное
  «нет в объявлении (kufar скрывает за капчей)».
- **`fetch_ad.py`** — до-фетч полной веб-версии: **kufar** текст+фото из `__NEXT_DATA__`
  `props.initialState.adView.data` (`body`/`gallery.images` — без капчи, за ней только телефон);
  **megapolis** описание из `.info_block_descripti*`, фото из og:image; **realt** описание из
  `.text-clamp*`, фото из **og:image** (это закрывает realt-фото для СОХРАНЕНИЯ, хотя для
  превью-карточек realt-фото по-прежнему нет). Сохранёнки → `web/saved/{хэш}/` (gitignore).
- UI: поиск/фильтры/сортировка (вкл. **фильтр «Площадь, м²» от/до** — два number-input,
  `state.areaMin/areaMax` в app.js; объекты без площади скрываются при заданном диапазоне),
  клик-копирование телефона, карта (**Яндекс.Карты**: метка
  по коорд, иначе поиск по адресу), на карточке «Сохранить» (→«Сохранено»-ссылка) и «Excel»,
  вверху ТРИ кнопки обновления (05.07.2026: «Обновить» = target `all`, «Обновить аукционы»,
  «Обновить банки» — Денис попросил вернуть раздельные; 01.07–05.07 была одна) —
  одна модалка с логом на всех (см. секцию про server.py `_update_steps`).
  Чекбоксы «С телефоном»/«С фото» УБРАНЫ (20.06, по просьбе Дениса).
  Кнопки карточки — в один ряд (Сохранить тянется,
  Excel/карта/сайт — иконки). Сегменты **Продажа / Аренда / Аукционы / Компании / Банки**.
- **Анализ v2: локация OSM + AI-вердикт (02.07.2026).** В модалке «Анализ» три новых блока:
  «Локация (OSM, 300 м)» (активность/магазины/общепит/аптеки + до транспорта 800 м),
  уточнения арендаторов по локации (правила на OSM-цифрах, `tenantTips` в app.js) и кнопка
  «AI-вердикт». Сервер: `GET /api/geo?hash=` (Overpass; без коорд → Nominatim-геокод по адресу)
  и `POST /api/verdict {hash,stats}` — **каскад бесплатных нейронок `_ai_providers()`:
  Groq gpt-oss-120b (мощнее, ~4 с, ключ `~/.groq_key`, обязателен браузерный UA — Cloudflare
  режет python-UA) → fallback GLM-4.5-flash (`~/.zai_key`)**. Кэши вечные: `web/geo_cache.json`,
  `web/verdict_cache.json` (ключ hash|цена — смена цены пересчитывает; оба в .gitignore; кэш
  ещё и В ПАМЯТИ сервера — сброс = rm файла + рестарт сервера). Сбои НЕ кэшируются.
  ⚠ Обе модели ОШИБАЮТСЯ, сравнивая числа сами («1358 выше 1615»), и путают семантику
  сырых ключей (transit_m=104 → «104 остановки») → в промпт идут ГОТОВЫЕ выводы
  (`позиция_к_медиане`, `cap_rate_оценка` из anaStats) с наказом брать дословно + говорящие
  русские ключи локации («метров_до_остановки»). После этого числа в вердиктах верные.
  ⚠ Грабли (пойманы на живых данных): (1) **GLM-4.5-flash по умолчанию «думает»** —
  reasoning_content съедает max_tokens → content ПУСТОЙ; лечится `"thinking":{"type":"disabled"}`.
  (2) **Nominatim не находит адреса с «г./ул./просп.» и с хвостом «, Беларусь»** — чистить
  префиксы regex'ом, страну передавать параметром `countrycodes=by` (проверено 4/4).
  (3) **Зеркала Overpass падают ПООЧЕРЁДНО** (то 504 у overpass-api.de, то таймаут kumi) —
  `OVERPASS_MIRRORS` перебираются по очереди (de → maps.mail.ru → kumi).
  (4) Кнопка в модалке НЕ ловится grid-обработчиком кликов (модалка вне grid) — отдельный
  listener на #anaBody. Рынок-статы для промпта собираются в `anaStats` при открытии анализа.
- **Банки в дашборде (20.06):** `export_data.load_banks()` читает ВСЕ `banks_*.xlsx` (glob,
  схема AUCTION_COLUMNS) → объекты `deal:"bank"`, сегмент «Банки», карточка `bankCard` (бейдж
  «Банк», тип, название, цена BYN, площадь, адрес, телефон, ссылка). Источники: `belinvest_bank.py`
  (Белинвест, 35 объектов залоговой недвижимости, серверный HTML) + оркестратор `collect_banks.py`
  (BANKS=[...], новый банк = новый `{name}_bank.py` + строка в BANKS, подхватится сам). Разведка
  17 банков (20.06): Приорбанк/ТоргКапитал/БНБ/БелВЭБ/Необанк/МТБанк — AJAX (реверс); РРБ/Цептер —
  серверные; Белгазпром/Альфа/rbank/БПС-Сбер/Дабрабыт — 403 (гео/антибот, проверить бел. IP).
  **Этап 2 сделан (03.07.2026, через петлю делегатов):** +3 парсера, сегмент 35→53 объекта:
  `belapb_bank.py` (Белагропром, bitrix-каталог 2 разделов, 19 недвиж.; фильтр «Вид имущества»
  содержит «жил» — иначе в залоговом разделе грузовики/оборудование; цен нет — «изучение спроса»;
  ядро GLM), `tcbank_bank.py` (ТК Банк, 2 крупных лота-аукциона в вордовских HTML-таблицах
  «метка|значение», всё 100%; ядро Groq + фиксы Claude: хвостовая точка предложения ломала
  float-цену, лоты-фантомы, nbsp в телефоне), `zepter_bank.py` (Цептер, 2 объекта; ⚠ JS-антибот
  `hg-security` — кука лежит В САМОМ челлендже, обход в 2 запроса в `_fetch`; ядро Claude — GLM
  трижды не осилил). **Гео-блок под VPN:** агрегаторы (benefit.by/myfin.by/ipmtorgi-банки) и
  РРБ/Приорбанк/БелВЭБ/МТБанк/Паритет/Сбер/Альфа/… отвечают только бел. IP → `bank_geo_probe.py`
  (автономный зонд, Денис гоняет БЕЗ VPN → bank_geo_out/) — по его HTML строить парсеры.
  Необанк/Технобанк — 404 (страницы переехали, искать заново).
  **Вердикт зонда (03.07, 11/14 страниц с бел. IP):** агрегаторы benefit/myfin = статьи-
  справочники, НЕ каталоги (объектов 0); их наводка — «все банк-залоги продаёт
  Белреализация» = beltorgi.by, наш парсер уже есть. Сбер = SPA (AJAX за бел. IP),
  Приорбанк = 1–2 объекта (76/77 «цен» — тарифы), остальные — пустые статики.
  **Гео-банки по наводке Дениса (03.07 вечер, сегмент 53→87):** Денис указал, что у
  Беларусбанка и БНБ объекты «глубоко» — подтвердилось. Зонд-КРАУЛЕР `bank_geo_probe2.py`
  (BFS 3 уровня по ссылкам property/imush/realiz, ⚠ БЕЗ VPN) нашёл: **Беларусбанк
  `/o-banke/property/client-property/nedvizhimost/` = 32 лота, ЛУЧШИЙ банк для обзвона —
  цена/адрес/ТЕЛЕФОНЫ С ИМЕНАМИ 100%** (структура div.item-lot: lot__title/status/
  descr_address/descr_price, droppable-поля «метка→голый текст-узел», фото swiper;
  20/32 с фото — у остальных нет на сайте); **БНБ `/o-nas/nashe-delo/realizuemoe-
  imushchestvo/` = 2 крупных объекта** (аккордеоны: <a class=accordion__head> + тело
  с таблицей характеристик и галереей; ресторан 3904 м² Могилёв + помещения Брест).
  Парсеры: `belarusbank_bank.py`, `bnb_bank.py` (ядро Беларусбанка — GLM в петле,
  фиксы Claude: float("...руб.") и голые текст-узлы droppable; ядро БНБ — Claude).
  ⚠ Оба ГЕО-БЛОК → НЕ в BANKS collect_banks (VPN); обновление: `bank_geo_collect.py`
  БЕЗ VPN (Денис) → ре-экспорт; флаг `--fixtures` = наполнение из страниц последнего
  зонда bank_geo_out2/ (использован для первичной загрузки 03.07). ✅ Слой 2 сделан
  (03.07): аукционы имущества САМОГО банка (bank-property, карточка = div.lot БЕЗ
  item-lot!) — листинг 2 стр. PAGEN → деталка-извещение; МНОГОЛОТОВЫЕ извещения
  (таблица «№|Наименование|цена|задаток», как mgcn) взрываются в отдельные лоты
  (`_money` отсекает номера лотов: значение <10 = не деньги). +6 строк, всего 38;
  2 деталки без фикстур (лимит краулера 40 стр.) — цены доедут живым прогоном без VPN.
  Хвосты (Сбер SPA, Приорбанк 1-2, РРБ мешанина) не окупаются (критерий 21.06);
  Дабрабыт/Белгазпром/Паритет — 404/500, страницы переехали.
- **Аукционы в дашборде (13.06):** `export_data.load_auctions()` читает вкладку «Аукционы»
  → объекты `deal:"auction"` (title/price BYN/deposit/adate/org/future). `parse_adate` ставит
  `future` (дата ≥ сегодня). В UI таб «Аукционы» по умолчанию скрывает прошедшие
  (чекбокс «Прошедшие аукционы»); карточка `auctionCard` — бейдж «Аукцион», название лота,
  цена BYN, выделенная дата (зелёная «ожидается» / серая «прошёл»), задаток, организатор.
  Из 2020 лотов актуальных (future) ~505 — остальные протухли по дате.
- ⚠ CSS-грабли (пойманы): класс с `display:` перебивает атрибут `[hidden]` (авторский стиль >
  UA) → модалка висела поверх и ела клики. Фикс: `[hidden]{display:none!important}`. Дочерний
  Python надо звать с `-u`, иначе stdout буферится и лог не течёт.
- `.gitignore`: `web/data.js`, `web/saved/`. Бэкап Яндекса синкает КОД `web/` (py/js/css/html/json/md,
  кроме data.js; кэши photos_cache/saved НЕ синкаются). ⚠ Починено 03.07.2026: до этого прежняя
  версия этой строки ВРАЛА — glob бэкапа брал только корень, web/ не попадал в Яндекс вовсе.

## История цен (02.07.2026)

`price_history.py` + врезка в `collect_realty.py` (Tracker): инкрементальный сбор сравнивает
цену каждого объявления ИЗ ЛИСТИНГА с базой — известные URL больше не «слепые». Покрыты
kufar/megapolis/gohome/byrealty (цена есть в листинге); realt и гео-источники (collect_geo) —
НЕ покрыты, TODO. Изменение → append в `price_history.json` + цена в базе обновляется на лету
(track мутирует prev_db ДО write_excel — итоговый xlsx несёт свежую цену, хэш/дедуп не трогаются).
Дашборд: `export_data.attach_price_changes()` вешает поле `pch` → бейдж «↓ цена снижена /
↑ цена выросла» рядом с ценой (тултип: старая цена + дата). Для обзвона «↓» = горячий лид.
⚠ **ГРАБЛЯ — курсовой шум (поймана на живых данных 02.07):** цены площадок двухвалютные, и
вторая валюта — ПЕРЕСЧЁТ по курсу НБРБ (первичен то $, то BYN: у евро-объявлений gohome рубль
стоял, € плыл). 13 из 14 первых «изменений» оказались шумом курса. Правило `changed()`:
реальное изменение меняет ВСЕ общие валюты; совпала хоть одна — шум, пропускаем.
Самопроверка: `./bin/python price_history.py` (13/13, вкл. живые кейсы шума).

## Главная команда

```
./bin/python collect_realty.py                    # все источники, инкрементально
./bin/python collect_realty.py --sources megapolis,kufar
./bin/python collect_realty.py --city belarus     # kufar по всей РБ (дефолт minsk)
./bin/python collect_realty.py --max-pages 5      # ограничить глубину
./bin/python collect_realty.py --full             # полный перепрогон (осторожно: затирает базу до собранного)
./bin/python doctor.py                            # health-check: кто из источников жив + доступны ли телефоны kufar (по IP)
```
Standalone-парсеры (`megapolis_parser.py`, `kufar_parser.py`, `realty_parser_v8.py --debug`)
работают по отдельности, пишут в свои файлы.

## Как устроено (общая логика)

- **Схема колонок** (COLUMNS в v8): `Тип, Телефон, Ссылка, Адрес, Район/Город, Площадь,
  Цена общая, Цена за м², НДС, Этаж, Год, Класс здания, Состояние, Парковка,
  Отдельный вход, Мокрая зона, Контакт, Имя контакта, Дата публикации, Источник,
  Высота потолков, Грузовая рампа, Эл. мощность, Витринные окна, Мин. срок, Материал, Хэш`.
- Excel-макет: закреплён столбец «Тип» + шапка (`freeze_panes="B3"`).
- **Инкрементал**: дедуп по URL (нормализованный). Известные URL пропускаются.
- **Стоп-пагинации**: ALL_KNOWN (вся страница уже в БД) или конец пагинации.
  DATE_STOP (дата < последнего прогона) есть в standalone-парсерах, но В ОРКЕСТРАТОРЕ
  ОТКЛЮЧЁН (last_run=None), т.к. общий файл имеет одну дату на все источники и
  DATE_STOP по чужой дате рано останавливал новый источник (баг был с Kufar — собрал 210 вместо 4636).
- **Чекпойнты**: запись каждые ~50-200 объектов → защита от потери при бане/сбое.
- **Подсветка**: новые с прошлого прогона строки заливаются жёлтым.
- **Классификатор «Здание»**: `is_building_text()` в v8 (фразы) + структурное поле
  «Тип объекта» на деталке. Тесты прошли 10/10.

## Текущее состояние данных

<!-- AUTO-STATE-START (обновляется автоматически в конце collect_realty.py) -->
`commercial_realty.xlsx` = **10451 объектов** (обновлено 10.07.2026):
- kufar.by: 5960
- domovita.by: 1453
- megapolis-real.by: 1260
- realt.by: 904
- bc.by: 767
- gohome.by: 101
- byrealty.by: 6
Телефоны: ~41%.
<!-- AUTO-STATE-END -->

### ⚠ АВАРИЯ 09.07.2026 — база затёрта (24326 → 1828). Причина найдена, фикс в коде

Цифра выше — НЕ реальное состояние рынка, а след аварии. Хроника из `BACKUP_INFO.txt`:
```
collect_geo:    24326 объектов, тел 59%   ← база целая
collect_realty:     0 объектов, тел  0%   ← записал ПУСТУЮ базу
collect_realty: 24448 объектов, тел 58%   ← пересобрал всё с нуля (~1 час)
collect_geo:     1828 объектов, тел 100%  ← снова затёр (1441 domovita + 387 bc)
```
**Корень (звено 1, ЧЕМ портился файл — доп. разбор 10.07 по наводке Дениса):** `write_excel`
делал `wb.save(боевой_файл)` НАПРЯМУЮ. xlsx — это zip: смерть процесса посреди save
(Денис закрывал программу; равно как свет/паника/OOM) оставляет **обрезанный архив**.
Проверено экспериментом `kill -9` посреди записи: старый код — **1 битая база из 5**
(3069 б вместо 50 000), новый — 0 из 5. ⚠ У Дениса база 24k строк → save длится СЕКУНДЫ,
окно попадания в разы шире, чем в тесте → «невезение» было закономерностью.
Особо: `kufar_phones.py` писал базу напрямую **чекпойнтом каждые 20 объявлений** часами —
самый частый писатель и главный кандидат в виновники (он же крутился в вечер аварии).
**Корень (звено 2, ПОЧЕМУ битый файл стирал базу):** `load_prev_db`/`load_prev_hashes` ловили
`except Exception` → печатали предупреждение и возвращали **пустую БД**. Вызывающий код пишет
`write_excel(prev_db + new)` → пустая БД = молчаливое стирание. **Триггер:** одновременная запись
в один xlsx (launchd-автодобор kufar 19:17–19:50 наложился на «Обновить» из дашборда) — но
одиночного закрытия программы посреди save хватило бы и без всякого наложения.

**Фикс атомарности (10.07): `R.atomic_save(wb, path)`** — save во временный сосед → `os.fsync`
(иначе при потере ПИТАНИЯ `os.replace` может опередить данные) → `os.replace` (атомарен: на месте
базы всегда либо целая старая, либо целая новая). Зовётся из `write_excel` (→ и все чекпойнты
collect_realty автоматически) и из обеих записей `kufar_phones`. Оборванный `.xlsx.tmp` безвреден,
перезапишется. Ирония: в `mgcn/ipmtorgi/fcon` атомарность была с июня — **главная база оказалась
защищена хуже второстепенных**. Проверка: `test_write_guard.py` кейс 6/6 (обрыв посреди записи).
**Фикс (10.07):** оба загрузчика при нечитаемом СУЩЕСТВУЮЩЕМ файле кидают `RuntimeError`
(запись отменяется) + защёлка в `write_excel`: отказ, если пишем < 50% (`SHRINK_LIMIT`) от
снимка `prev_hashes`; намеренный перепрогон — `allow_shrink=True` (пробрасывается из `--full`
в `collect_realty`). `collect_geo` правки не требует: он всегда пишет `база + новые` → защёлка
ловит его сама. Проверка: `./bin/python test_write_guard.py` (5/5, воспроизводит аварию).

### Журнал телефонов `phones_log.py` (10.07.2026) — единственная копия невосстановимого

Объявления пересобираются за час, **телефоны kufar — нет** (бел. IP + логин Chrome + дневная
квота ≈ месяц добора). 09.07 их погибло ~2600, т.к. жили ТОЛЬКО в ячейках xlsx.
`phones_log.jsonl` — **append-only**, не переписывается никогда → стереть перезаписью нечем.
Врезан в `kufar_phones.py`: номер пишется в журнал **ДО** xlsx (+`fsync`).
- `--dump` (занести телефоны из базы; сделано 10.07: **4337**) | `--restore` (залить в ПУСТЫЕ
  ячейки, заполненные не трогает) | `--status` | `--selftest` (4/4).
- Живая проверка 10.07 на копии базы: стёрли 4337 → `--restore` вернул 4337, сверка с оригиналом
  10448 строк, **0 расхождений**.
- ⚠ В `.gitignore` (перс. данные, репо публичный). Бэкап Яндекса чинён: glob брал `*.json`,
  а журнал `.jsonl` → **не уезжал бы вовсе**; добавлен `*.jsonl`.
- ⚠ Две грабли, пойманные самопроверкой (обе реальные): (1) `def load(path=LOG)` — дефолт-аргумент
  вычисляется ОДИН раз при импорте, подмена `LOG` в тесте не действует → `path=None` + `path or LOG`.
  (2) Оборванная последним обрывом строка не имеет `\n` → следующий `append` **склеивается с
  огрызком и гибнет вместе с ним** (обрыв убивал бы ДВЕ записи) → `_unterminated()` дозакрывает `\n`.

**Что НЕ сработало / чего не делать:**
- **Наш «бэкап» — зеркало, а не бэкап.** Яндекс синкается ПОСЛЕ прогона → битый файл уехал в облако
  и вытеснил здоровый. История версий Яндекса дала только 1.6 МБ от 30.05 (не наша база), корзина
  пуста. `.bak` рядом с xlsx делается перед записью → тоже уже битый.
- **Дубль (`snapshot_db.py`) НЕ заменяет журнал, и наоборот.** Дубль спасает ФАЙЛ целиком (и то
  лишь от схлопывания/порчи), журнал спасает НЕВОССТАНОВИМОЕ поле. Нужны оба.
- Time Machine на маке НЕ настроена, локальных снапшотов APFS нет — откатываться было некуда.
- Объявления (24k) не страшно: пересобираются `collect_realty` примерно за час.
  **Потеряны ~2600 телефонов kufar** (месяц добора по ~100/день) — они жили ТОЛЬКО в строках xlsx;
  ни в `.kufar_nophone.json`, ни в `price_history.json`, ни в кэшах их нет.
- ⚠ Пока база не восстановлена, оба launchd-агента ВЫГРУЖЕНЫ (`com.realty.kufar-autophones`,
  `by.realty.weekly`). Вернуть: `launchctl load ~/Library/LaunchAgents/<plist>`.

**Сделано следом (10.07):**
- **Замок базы** `R.acquire_db_lock(path)` (`fcntl.flock`, неблокирующий) в `collect_realty`,
  `collect_geo`, `kufar_phones` — второй пишущий падает с внятной ошибкой вместо порчи файла.
  Замок держится до конца процесса, ядро снимает его при kill/краше. Проверен на двух процессах.
- **Дубль базы** `snapshot_db.py` → `commercial_realty_дубль.xlsx`. Это НЕ зеркало: копия
  обновляется, только если основа читается И не схлопнулась (< `SHRINK_LIMIT` от дубля → отказ).
  Зовётся сам в конце `collect_realty` (функция `update()`, БЕЗ argparse — `main()` с argparse
  съел бы чужие флаги и убил процесс через `SystemExit`). Ручной: `./bin/python snapshot_db.py`,
  состояние — `--status`.
- **`Восстановить базу.command`** — автономный лаунчер для Дениса БЕЗ VPN (doctor → collect_realty
  → collect_geo → дубль → export).

⚠ **Восстанавливать `collect_realty.py` БЕЗ `--full`** — иначе обнулится база и уцелевшие
гео-строки (domovita/bc) пропадут: этот оркестратор их не собирает.

⚠ **ГРАБЛЯ ЗАПИСИ О ГЕО-БЛОКЕ (снята 10.07 вечером).** Здесь стояло «kufar и realt не
отвечают под VPN, восстанавливать только БЕЗ VPN» — это был **снимок одной неудачной
минуты, а не правило**: тот `doctor.py` 3/5 совпал с временным сбоем. Повторный прогон
17:45 из-под того же UK-IP: **kufar ✅, realt ✅, gohome ✅, byrealty ✅**, лёг megapolis
(SSL invalid session id — его обычный сетевой бан за всплеск). Гео-блок реален только для
**телефонов kufar** и гео-источников (domovita/edc/bc/банки) — листинги realt/kufar под
VPN собираются. Урок: единичный отказ источника ≠ гео-блок; прежде чем писать правило в
CLAUDE.md — прогнать `doctor.py` дважды.

Важные оговорки (вручную, авто-блок их не трогает):
- realt.by: 5103 = продажа 2243 + аренда **2860** (обе собраны; спот-чек 04.06 — строки листа
  «Аренда» реально `rent-shops/rent-offices/...`). Прежняя заметка «аренда недобрана ~295» была
  СТАЛОЙ и самопротиворечивой (5103 не сходится с «295 + полная продажа») — снято.
  ✅ Пробел «Цена общая» у аренды ЗАКРЫТ (05.06): у realt-аренды цена = ставка/м², итог был пуст
  у 56% (1625). Итог = ставка × площадь → бэкфилл существующих (1622, fill 44%→99%) + парсер v8
  считает на лету (`rent_total_from_m2`, ПОСЛЕ хэша — дедуп-ключи не тронуты). Битые «Цена за м²»
  (2%, склеены 2 числа) → берём только USD-итог. 3 строки реально без цены.
  ⚠ **ГРАБЛЯ склейки цены realt (09.07.2026, наводка Дениса «Аранская 8 vs Гикало 1»):**
  ценовые regex в `parse_listing_text` брали `([\d\s]{2,})` — жадный класс `[\d\s]`
  склеивал соседнее число через ОДИН пробел (этаж/этажность + цена): «7 18 309 р.» =
  этаж 7 + 18 309; «9 2 640 000 р.» = 9 + 2 640 000. Признак битости: настоящая цена
  группируется по 3 справа (718 309), а «7 18 309» = 1+2+3 цифры ≠ разделитель тысяч.
  Фикс: во ВСЕХ 4 regex (рубли/ставка-м²/$/$-м²) `[\d\s]{2,}` → `\d{1,3}(?:\s\d{3})+|\d+`
  (ОДНО число). Бэкфилл базы: 235 «Цена общая» починены отрезанием ведущей цифры
  (остаток проходит группировку-по-3; бэкап `.xlsx.bak`). «Цена за м²» (458 битых типа
  «31 70») НЕ бэкфилчены — обе группы коротки, какая настоящая неоднозначно → починятся
  сами при рефреше realt пофикшенным парсером. **Отдельно:** адрес «Аранская 8» был ВЕРЕН
  на момент сбора — объявление realt (`object/4164982`) ПЕРЕИЗДАНО под тем же URL/ID
  (агент сменил объект на Гикало 1); наши данные = снимок, лечится incremental-рефрешем,
  НЕ баг парсера адреса (соседние Аранская-8 объекты сверены с сайтом — совпали).
- kufar.by: аренда упёрлась в max_pages=100 (≈2980), возможно есть ещё.
- megapolis-real.by: полный.
- Низкий % телефонов тянет вниз Kufar (без телефонов).

## Открытые задачи / TODO

1. **realt-аренда — УЖЕ собрана (2860, проверено 04.06), задача снята как выполненная.**
   Осталось по realt: (а) периодический incremental-рефреш `./bin/python collect_realty.py
   --sources realt` для свежих листингов; (б) ✅ пробел «Цена общая» у аренды ЗАКРЫТ (05.06,
   бэкфилл 1622 + `rent_total_from_m2` в v8 — итог = ставка/м² × площадь).
   ⚠️ realt.by банит примерно каждые ~2500 карточек — может встать; перезапуск
   продолжит с чекпойнта. Возможно нужны бо́льшие паузы между карточками.
   **Анти-бан в `realty_parser_v8.py` (03.06, идеи Qwen, починены/встроены мной):**
   ротация User-Agent (`USER_AGENTS`/`get_random_ua`), детектор блокировки
   `is_blocked` (Cloudflare/WAF → пауза+повтор в `goto_with_retry`), человеческий
   скролл `human_scroll`, сброс контекста браузера каждые 500 карточек (новый
   fingerprint). Смоук пройден (67 объектов с листинга). ⚠ Баги Qwen, которые я
   выловил: `\|` вместо `|` ломал ВСЕ альтернативы-регэкспы; `await page.content()[:N]`
   резал корутину (is_blocked был мёртв). В нашей версии исправлено.
2. **Megapolis** банит IP на сетевом уровне (TCP-дроп) при всплеске запросов.
   Паузы уже подняты (3-6с/страница, 4-8с/категория). Если забанен — ждать часы.
3. **Телефоны Kufar — РЕШЕНО (12.06).** ⚠ Корень был НЕ в reCAPTCHA, а в **ГЕО-БЛОКИРОВКЕ
   ПО IP**. Прежние выводы (31.05 «нет», 05.06 «за капчей») сделаны ИЗ-ПОД Psiphon → kufar
   видел иностранный IP и прятал номер. Открытие пользователя: с белорусского IP (VPN OFF)
   номер виден глазами, а с VPN — нет. Подтверждено зондом: наш IP под Psiphon = Нидерланды
   (Scaleway, hosting); с бел. IP кнопка **«Позвонить»** (НЕ «показать телефон») сама проходит
   невидимую reCAPTCHA v3, а эндпоинт **`GET api.kufar.by/search-api/v2/item/{id}/phone`**
   отдаёт чистый `{"phone":"375…"}`. Капча-solver/логин НЕ нужны.
   ⚠ **ОБНОВЛЕНИЕ 14.06: kufar ДОБАВИЛ ЛОГИН-СТЕНУ** — телефон теперь за обязательным входом
   («войдите, чтобы увидеть телефон»); старый способ без логина даёт `no_response`. Вход в
   автоматике невозможен (kufar-логин у юзера через Google, Google блокит автоматизацию).
   РЕШЕНИЕ (проверено 5/5): флаг **`--chrome-cookies`** тянет залогиненную сессию kufar из
   обычного Chrome (browser_cookie3 → k_jwt/session) + playwright-stealth. Запуск:
   `kufar_phones.py --limit N --chrome-cookies`. Держать аккаунт залогиненным в Chrome.
   ⚠ Автодобор launchd сам НЕ сработает (нужен Chrome-логин + связка ключей) → kufar-телефоны
   теперь РУЧНЫЕ. Новая зависимость: browser_cookie3.
   • Инструмент: **`kufar_phones.py`** (Playwright) — берёт kufar-строки без телефона из
     xlsx, открывает деталку, жмёт «Позвонить», ловит ответ `/phone`, дописывает в колонку
     «Телефон». ⚠ **ЗАПУСКАТЬ С ВЫКЛЮЧЕННЫМ Psiphon** (иначе гео-блок). Бэкап `.xlsx.bak`,
     чекпойнты/20, паузы 2.5-6с, сброс контекста/250, естественный резюм (берёт пустые).
     `--limit N` (дробить!), `--headless`. ~8.3с/объявление → полный добор ~12.7ч, гнать
     порциями 300-500 в разные часы (ToS-серо, темп человеческий).
   • `split_phones()` — у объявления бывает 2-3 номера, kufar отдаёт слитно → режем по
     канону `375\d{9}`, пишем через запятую (юнит 6/6). Смоук 12.06: 21/25 за 208с.
   • Осталось добрать ~5497 (на 12.06 kufar с телефоном = 21). ~7% держат номер и в тексте
     (`phones_from_text` в web/server.py — добор без VPN не нужен).
   • Доработки 13.06: `--login` (persistent-профиль `.kufar_profile`, юзер входит сам —
     залогинен = выше лимит раскрытий) | кэш `.kufar_nophone.json` (хэши без телефона —
     no_button/404 больше не перепроверяются; `remaining_count()` считает остаток) | get_phone
     различает причины (ok/no_button/no_response/throttled) | BAN_STREAK=12 → авто-стоп при
     серии отказов | `split_phones` режет слитые номера. ⚠ Реальность kufar: придушивает после
     ~165 раскрытий/сессию (проверено: у «пустых» в 7/9 случаев телефон ЕСТЬ) → гнать малыми
     порциями (150-200) с перерывами.
   • **Автодобор (13.06):** `kufar_auto.py` + launchd `com.realty.kufar-autophones.plist`
     (StartInterval 4ч) + лаунчеры `Автодобор-вкл/выкл.command`. Каждый заход: guard бел. IP
     (не BY → пропуск, лог `logs/kufar_auto.log`) → **шаг 1: сбор свежих объявлений
     (`collect_realty.py --sources kufar`)** → `remaining_count()` (0 → «заполнено», стоп)
     → шаг 2: порция 150 (`--headless --force`) → ре-экспорт. Реально работает ТОЛЬКО при
     Psiphon OFF. ⚠ headless для kufar reCAPTCHA — ✅ ПРОВЕРЕН вживую 05.07 (21/25 = 84%,
     как с окном), `--headless` в автодоборе оставлен.
   • **«Свежие первыми» (05.07.2026, просьба Дениса):** дневная квота kufar дефицитна →
     заход сперва собирает НОВЫЕ объявления (`collect_realty --sources kufar` — new-строки
     без телефона дописываются в КОНЕЦ базы: `write_excel(base + all_new)`), а `collect_targets`
     обходит лист **С КОНЦА вверх** (`range(ws.max_row, 2, -1)`) → свежие лиды добираются
     первыми, остаток квоты уходит на старый хвост. Встроено в ОБА захода (`kufar_auto.py`
     шаг 1/2 и `Добор kufar.command` шаг 0/3). Проверено на живой базе: targets идут
     6537→6536→… (с конца), а не 3→4→… . ⚠ read_only-openpyxl для тешка НЕ годится
     (random-access `ws.cell` дико медленный) — грузить в память, как реальный kufar_phones.
   • **05.07.2026: добор возобновлён и автодобор ВКЛЮЧЁН.** ⚠ Грабля: `kufar_auto.py`
     писался 13.06 — ДО логин-стены 14.06 — и звал добор БЕЗ `--chrome-cookies` (дал бы
     сплошные no_response); починен. Новый лаунчер **`Добор kufar.command`** (Денис, БЕЗ VPN,
     Chrome залогинен): смоук headless 25 → сам выбирает headless/окно (порог 15/25) →
     порция 150 → ре-экспорт; лог `logs/kufar_dobor.log`. Живой прогон 05.07: смоук 21/25,
     порция 76/135 → **+97 номеров за заход**; throttle пришёл после ~97 раскрытий за утро
     (BAN_STREAK остановил чисто, резюм добёрет) → порции ~150 с перерывами в часы = верный
     темп. Автодобор launchd **ЗАГРУЖЕН 05.07** (com.realty.kufar-autophones, раз в 4ч,
     под VPN заход скипается сам, лог `logs/kufar_auto.log`). Остаток 9032, кэш
     «телефона нет» = 193. Темп ≈ 97/заход → полный добор ~месяц фоновых заходов
     (реально быстрее в дни, когда VPN подолгу выключен).
   • **Замер интервала (05.07, ЗАВЕРШЁН → вернул 4ч):** гоняли StartInterval 1ч, каждый
     заход писал строку в `logs/kufar_stats.csv` (время,порция,добыто,no_response,no_button,
     throttled,error,бан_стоп,минут,остаток — save_stats в kufar_auto.py, парсит отчёт
     kufar_phones). **Итог дня:** утром ручной `Добор kufar.command` дал +97 (смоук 21/25),
     потом throttle; дневные АВТО-заходы почти пусты — 12:50 (+4ч после утра) **добыто 5**,
     13:58 (+1ч) **добыто 1**, оба умерли по BAN_STREAK через ~15 объектов (12 no_response
     подряд). **Вывод: лимит kufar ДНЕВНОЙ на аккаунт/IP, а не «пауза N часов».** Утренний
     сильный заход выбирает квоту → дальше в этот день почти ноль, сколько ни долби → частый
     интервал бесполезен (только держит троттл-стрик тёплым). **Постоянный интервал вернул на
     4ч (14400с)** в plist + `~/Library/LaunchAgents` + reload. Реальный рабочий паттерн =
     ОДИН сильный заход/день утренним ручным `Добор kufar.command`; автодобор launchd = фоновая
     подчистка, когда VPN подолгу выключен. ⚠ launchd НЕ стреляет, пока мак спит.
4. **Kufar глубже max_pages=100** — поднять лимит, если нужна полная аренда.

## Идеи (что ещё НЕ сделано)

- **Полное описание объявления** при сохранении — сейчас save_marked сохраняет
  только то, что уже в строке. Чтобы добавить полный текст описания, нужно
  re-fetch детальной страницы (megapolis/realt — можно; kufar — за антиботом).
- **Рейтинги/отзывы Яндекса** на локации — нужен Yandex API-ключ (Geosearch).
  Бесплатный лимит ~1000/день — хватит для отмеченных. Сейчас НЕ задан.
- **AI-вердикт по локации** (текстовая сводка «хорошее место под общепит, трафик
  средний, конкуренция высокая…») — нужен `ANTHROPIC_API_KEY`. Сейчас НЕ задан.
  💡 Когда будем слать Claude ПАЧКУ объектов (напр. «оцени эти 50 локаций»),
  упаковывать не `json.dumps(rows)`, а компактно «схема + строки» — режет ~64%
  входных токенов БЕЗ потери ключевых полей (проверено на headroom/SmartCrusher,
  04.06; ссылки/телефоны выжили 100%, фото-URL ~11% подрезает). Свой хелпер,
  без зависимостей:
  ```python
  def listings_to_csv_block(rows: list[dict]) -> str:
      keys = list({k for r in rows for k in r})
      head = "|".join(keys)
      body = "\n".join("|".join(str(r.get(k, "")) for k in keys) for r in rows)
      return f"{head}\n{body}"   # это в промпт вместо json.dumps(rows)
  ```
- **Backfill Фото URL/Координаты для существующих 9142** объектов — у kufar/megapolis
  данные есть в источниках, но текущие строки собраны до фичи. Нужен спец-проход.

## Стиль работы с пользователем

- **Пользователя зовут Денис. Обращаться к нему по имени в КАЖДОМ ответе.** (правило от 19.06.2026)
- Общение на русском.
- Пользователь просил НЕ переспрашивать разрешение — действовать самостоятельно по заданной последовательности.
- Длинные сборы запускать в фоне, не опрашивать, ждать уведомления.
- Перед записью в xlsx убеждаться, что файл закрыт в Excel (иначе не сохранится; признак — файл `~$commercial_realty.xlsx`).

## GitHub-репо + связка с Qwen (03.06.2026)

**Репо:** `https://github.com/Bakuka17/parser-realt` (ветка `main`). Только КОД (venv/xlsx/logs
в .gitignore). **Claude пушит напрямую** — токен пользователя закэширован в macOS Keychain,
`git push origin main` из среды Claude проходит БЕЗ запроса пароля. **Qwen — только READ**
(делает `git pull`, гоняет код, исполнять/коммитить не может). Модель связки: **Claude пишет
код + ведёт CLAUDE.md → пушит сам → Qwen подтягивает и прогоняет.** Qwen присылает свои правки
ЧЕРЕЗ пользователя (в чат) — Claude их QA-ит (на исполнении, не на слово!) и коммитит.
⚠ Типовое: Qwen пишет «все тесты пройдены», а по факту нет (ловил 6/8, 6/10, 9/10) — ВСЕГДА
прогонять самому. Бэкап диалогов: `dialogs/` (в .gitignore, синк в Яндекс).

**Qwen = ВЕБ-чат (chat.qwen.ai), НЕ локальный CLI (04.06).** Пользователь работает в веб-Qwen,
а не в Qwen Code CLI (CLI запустить не смог). Это критично для модели связки:
- Веб-Qwen **НЕ может запускать наш код** — `code_interpreter` у него в облачной песочнице
  Alibaba, без нашего репо/venv/доступа к нашим сайтам. Значит «Qwen проверил, работает» из
  веба ≠ проверено. **Финальный прогон против живых данных — ВСЕГДА на Claude (или юзере
  в терминале).** Отсюда и хроническое «тесты пройдены», а по факту нет — он физически не
  гоняет код у нас.
- «Глаза» у веб-Qwen ЕСТЬ: `web_extractor`/`fire-crawl` (чтение веб-страниц) — наказ Qwen:
  «сначала открой URL через fire-crawl/web_extractor, потом пиши регэксп» (лечит `<strong>`/`<b>`).
- MCP в веб-Qwen добавляются ТОЛЬКО как удалённые URL (SSE/HTTP) — локальные stdio-команды
  (`uvx`/`npx`) облако не запустит. Добавлен **Context7** по URL `https://mcp.context7.com/mcp`
  (живые доки библиотек). fire-crawl/code-interpreter/Sequential-Thinking — встроенные тумблеры.
- Локальный Qwen Code CLI БЫЛ настроен (fetch/context7/excel/ddg), но **удалён за ненадобностью**
  (`~/.qwen/settings.json` пуст) — юзер CLI не использует. uv оставлен (на нём `ruff`).

**Линтер: ruff (Astral) — `ruff.toml` в репо (04.06).** Запуск: `uvx ruff check .` (uv стоит
в `~/.local/bin`). Конфиг заточен под нас: `select = F,B` (реальные баги/мусор — unused/
undefined-имена, bugbear), НЕ берём E701/E702/E401 — это намеренный компактный стиль проекта
(`if x: continue`, `a; b`), не ошибки. Репо зелёный: вычищен мёртвый код, найденный ruff —
3× осиротевший `mp`-regex (auction24/torgi24/ipmtorgi: цена и так шла через `extract_start_price`),
`addr_blob` в eauction, unused-import в save_marked. Опц. гейт на коммит — `.pre-commit-config.yaml`
(lint-only, без `--fix`/format чтобы не ломать стиль; активировать: `uv tool install pre-commit
&& pre-commit install`). **Serena/LSP-MCP Qwen НЕ ставим** — на мелком репо её ~25 инструментов
раздули бы контекст Qwen (потеря ликвидности); окупается на больших кодовых базах, не на нашем.

## Договорённости о делегировании (зафиксировано 01.06.2026)

**Сделка:** пользователь использует платную подписку ТОЛЬКО с Claude. Claude взамен —
честно оценивает пользу других нейросетей (не занижает и не завышает) и грамотно
делегирует им подходящие куски, оставаясь дирижёром и контролёром качества.

**Роли:**
- **Claude (я)** — дирижёр + QA + сложные куски: реверс JS/API, схема/контракт
  (`auctions_common.py` — единый источник правды), интеграция, финальная проверка
  КАЖДОГО результата на живых данных (не верить «fill rate» и «тесты пройдены» на слово —
  всегда запускать и спот-чекать реальные значения).
- **z.ai (Zhipu GLM)** — ОСНОВНОЙ чат-делегат для КАРКАСОВ парсеров (чистые серверные сайты) +
  логики (решение Дениса 21.06.2026: заменил Qwen, т.к. GLM-Flash ПОЛНОСТЬЮ бесплатный, 203K
  контекст — Qwen по сути платный, упирается в лимиты). chat.z.ai. Те же грабли, что у любого
  веб-чата: НЕ запускает наш код против живых сайтов → «работает» ≠ проверено, финал QA на мне.
- **Qwen** — теперь РЕЗЕРВ (по сути платный/лимиты). Прежняя оснастка (web_extractor/Context7) — ниже.
- **DeepSeek** — чистые самодостаточные логик-функции с тестами (напр. extract_area — сделал 11/11).
- **Gemini / Grok** — резерв, если у первых лимит. Слабее на парсерах.

**Что делегировать:** болванки парсеров для чистых серверных сайтов; чистые логик-функции;
однотипные вариации по образцу.
**Что НЕ делегировать (делаю сам):** реверс JS/API; схему/контракт; интеграцию; финальное QA.

**Почему боты ошибаются (структурно, не от тупости):** в чат-режиме они НЕ могут сами
запустить код против живого сайта и поправиться → пишут вслепую. Типовые баги, которые
я всегда проверяю и чиню: (1) неверные ключи словаря (нужны буква-в-букву из COLUMNS),
(2) регэксп ссылок не ловит абсолютные URL, (3) цена/поля хватают первый попавшийся мусор
при «100% заполнении», (4) ложное «тесты пройдены». Стандартный приём: давать им
«закалённый бриф» (точный API-контракт + точные ключи + «сначала curl, потом код»).

## Проект «Аукционы» (в работе, 01.06.2026)

Отдельная ветка: парсеры аукционных площадок РБ. Каркас — `auctions_common.py`
(схема AUCTION_COLUMNS, fetch, parse_price/parse_date/**normalize_price**, extract_area/
extract_address/**extract_re_address**/extract_start_price/**clean_auction_description**/
classify_object_type/extract_lot_number, write_excel→лист «Аукционы», авто-колонка «Тип объекта»).
Пишут в отдельные `auctions_{site}.xlsx`, потом `merge_auctions.py` сводит в `auctions_realty.xlsx`
с дедупом по URL. Образец парсера: `mgcn_auctions.py`.

⚠ **ГРАБЛЯ ДЕДУП-КЛЮЧЕЙ (поймана 04.07.2026, чинена в auctions_common):** `load_prev`
ключевал URL СВОЕЙ формулой (rstrip("/") без unquote), а сбор сверял через `norm_url`
(слэш сохраняет) → у mgcn (ссылки со слэшем, вдобавок в двух кодировках — %d0… и
кириллица) ключи НИКОГДА не совпадали: каждый инкремент пересобирал все лоты и файл
стабильно удваивался (629=315×2). Фикс: load_prev ключует ТОЛЬКО через norm_url;
norm_url делает unquote. Единый источник правды ключа — norm_url, никаких локальных формул.

**МГЦН-аренда (04.07.2026):** mgcn ВСЕГДА собирал 3 категории (sale/rent/place), но
parse_detail игнорировал deal_type — всё писалось «Аукцион». Теперь «Тип торгов» =
«Продажа с аукциона / Аренда с аукциона / Аренда земли» (315: 169/102/44); дашборд:
`dealKind` в export_data (COLMAP_AUC «Тип торгов»), бейдж аренды на карточке аукциона,
аренда ищется поиском. bc.by (наводка Дениса) ✅ ИНТЕГРИРОВАН 04.07 как гео-источник
АРЕНДЫ (см. секцию «bc.by» выше); аукционы ГХУ `?id=79` — открытый TODO там же.
⚠ Уроки первого прогона зонда по bc: (1) bc.by отдаёт **windows-1251**, а fetch зонда
делал `decode("utf-8","ignore")` → вся кириллица выброшена из сохранённых страниц
(«re:0 price:0» в отчёте — симптом именно этого); (2) ключ дедупа зонда не резал схему →
http/https качались как разные страницы, полбюджета на дубли. Оба пофикшены в
bank_geo_probe2.py (charset из заголовка/меты + `norm()`; выбор целей аргументом:
`./bin/python bank_geo_probe2.py bc`).

**Главная команда аукционов:** `./bin/python collect_auctions.py` — единый оркестратор:
одна команда гоняет ВСЕ площадки (каждую отдельным процессом) + merge → один свод.
Флаги: `--sources mgcn,torgigov`, `--skip beltorgi`, `--mgcn-full`, `--no-merge`.
Его же зовёт `scheduled_run.sh` (недельный launchd).

**Всё в одном файле (05.06): `embed_auctions.py`** встраивает свод `auctions_realty.xlsx`
вкладкой «Аукционы» в `commercial_realty.xlsx` (рядом с Сводка/Продажа/Аренда — чтобы не бегать
по файлам). Вызывается в конце ОБОИХ оркестраторов: `collect_auctions` (после merge) и
`collect_realty` (realty пересоздаёт файл через `Workbook()` → вкладку надо восстанавливать там же).
Вручную: `./bin/python embed_auctions.py`. Существующая вкладка пересоздаётся, остальные листы целы.

Доп. хелперы (через делегирование Qwen/DeepSeek, проверены/исправлены мной):
`normalize_price` (Qwen, 17/17 — строка цены → (float, валюта)); `extract_re_address`
(DeepSeek→правка, 10/10 — адрес ОБЪЕКТА, не офиса); `clean_auction_description`
(DeepSeek→правка — срезает НДС-болванку/извещение/контакты у описаний torgi.gov/deloocenka).
mgcn использует свои `mgcn_address`/`mgcn_area` (брал офис «К.Маркса 39» — пофикшено,
адрес 8→30%, площадь 15→86%).

Цена тянется через `extract_start_price` (прицельно по «начальная/стартовая цена … число …
BYN/бел.руб», схлопывает переносы строк).
**mgcn-цена/задаток (04.06): `mgcn_price`/`mgcn_deposit` + `_table_money_range` (bs4).**
У mgcn ДВА формата деталки: (1) одиночный лот — цена/задаток в прозе (ловит
`extract_start_price`/regex); (2) МНОГОЛОТОВЫЙ аукцион — HTML-`<table>`, где у каждого
лота своя строка, а цена/задаток в колонках. Шапка таблицы часто во ВТОРОЙ строке
(первая — colspan «Сведения о…»), название колонки варьируется → ищем ячейку-шапку с
«начальн»+«цен» (приоритет «…предмета аукциона» = итог с НДС) и «задат», берём всю
колонку по строкам-лотам и отдаём **диапазон min–max BYN** в ОДНУ строку аукциона.
На живом смоуке цена+задаток заполнились 6/6 (раньше многолотовые = пусто). ⚠ Одна
строка = весь многолотовый аукцион (диапазон), НЕ по лоту; если нужны отдельные лоты —
взрывать строки таблицы в отдельные item'ы (пока не делаю). Реверс `apiCall`-эндпоинта
НЕ понадобился — это generic-бойлерплейт larek (только `visit-logout`), цены в статике.
**Организатор-канон (04.06):** regex ловил мусор на 33% страниц («документ», «электроснабжение»);
у mgcn организатор ВСЕГДА сам центр → если в захвате нет «МГЦН», ставим
«государственное предприятие «МГЦН»». После `--full`+бэкфилла: 298/298 корректны.
Итог пересбора `--full` (04.06, со всеми фиксами): 298 лотов, **цена 97% / задаток 97% /
адрес 100% / тел 100% / орг 100% / дата 100%**; площадь 86% (остаток — земля в га = N/A).
**Чекпойнты+резюм (04.06):** `--full` сохраняет прогресс каждые 20 лотов в `auctions_mgcn.tmp.xlsx`,
финал — атомарный `os.replace(tmp→out)`; при обрыве рестарт `--full` подхватывает `.tmp` и
добирает остаток (collect() принимает skip_urls). Решило боль «умирал на 215/298, терял всё».

**Свод `auctions_realty.xlsx` = 1859 уникальных лотов (10 источников, обновлено 02.06.2026):**
ipmtorgi(891), **torgi.gov(350)**, mgcn(296), eauction(142), torgi24(83), auction24(36),
**deloocenka(23)**, bks(16), gki(16), konfiskat(6). beltorgi — 0 (см. ниже).

**Парсеры по типу:**
- Серверный HTML: mgcn, ipmtorgi, torgi24, auction24(событийный), gki/bks(грязные),
  **fcon** (fcon.by/auctions/nedvizhimost — ЭТП «Фонд», JoomShopping; карточки blockProductItemLot:
  заголовок=тип+город+площадь, адрес в `auctionAddress title`, дата/лот/фото; деталь даёт
  нач.цену/задаток/телефон. ⚠ **фильтр актуальности** `_is_archive`: берём лот ТОЛЬКО если статус
  не «не состоял/продано/отменён/заверш» И дата ≥ сегодня — у fcon вся недвижимость сейчас архив,
  актуальных 0, как beltorgi; парсер подхватит новые. Пагинация — первая страница ~26 лотов,
  доп. через AJAX `productsAuctionMore`, пока YAGNI),
  konfiskat, **deloocenka** (deloocenka.by/aukzioni — оценочная фирма в Гомеле; вкладки
  #menu1/#menu2 активные, #menu3 проданное-пропускаем; позитивный фильтр недвижимости
  по ТИПУ из заголовка, дата только из листинга).
- Серверный список + JS: **eauction** (только разделы `/nedvizhimost/` — в общем `/commerce/`
  намешаны авто/станки!).
- **torgigov** (`torgigov_auctions.py`) — ЧИСТЫЙ JSON-API `api.torgi.gov.by/api/lots?category=1`
  (=недвижимость). Сервер НЕ фильтрует активные → лоты сорт. по auctionStart убыв., берём
  state∈{AuctionPublished,AuctionPublishedEgrsb} с page=1, стоп после 3 пустых страниц.
  ⚠ **`initialPrice` в КОПЕЙКАХ → /100 = BYN** (подтверждено правдоподобием). Организатор/телефон
  API не отдаёт (только id) — пусто, контакт = ССЫЛКА на аукцион (торги через площадку, как kufar).
  Заполняет колонку **«Описание»** (поле `description` из API, срезана болванка-преамбула про НДС) —
  для torgi.gov важны лот + описание. Колонка «Описание» добавлена в AUCTION_COLUMNS (перед «Хэш»).
- **beltorgi** (`beltorgi_auctions.py`) — ЗАО «Белреализация», банкротные торги. Листинг через
  AJAX → рендер Playwright (`/aukciony/?status=...`); деталь `lot-{id}-{n}.html` серверная
  (адрес/задаток/организатор/телефон есть, СТАРТ-ЦЕНА динамическая=JS, часто пусто).
  Фильтр недвижимости по 2-й хлебной крошке. **Проверено 13.06: 0 лотов даже в DOM**
  (площадка смешанная, недвиж-разделы пусты) — парсер отрабатывает чисто, подхватит, когда появятся.
- **butb — ЗАКРЫТО как неприменимое (13.06).** `et.butb.by` — пустая SPA (статика 95 байт,
  данные за JS-API), а БУТБ это товарно-сырьевая биржа (металл/лес/сельхоз) — недвижимость
  непрофильна и в категориях не найдена. Реверс SPA-API ради вероятного нуля не оправдан;
  парсер НЕ пишем. Вернуться только если пользователь подтвердит, что недвижимость там есть.

**Разведка списка площадок от Дениса (20.06.2026, 18 ссылок).** Уже покрыты: gki, torgi.gov,
e-auction(/nedvizhimost), beltorgi, ipmtorgi, bks. Итог проверки новых:
- ✅ **fcon.by** — ИНТЕГРИРОВАН (`fcon_auctions.py`, серверный, 26 лотов недвиж., но все архивные →
  фильтр актуальности, сейчас 0; см. выше).
- ❌ **Облисполкомы gomel.gov.by / grodnorik.gov.by / brest-region.gov.by** — НЕ лоты, а PDF-извещения
  и текстовые анонсы (grodno: 73 PDF; brest: ссылки ведут на gki.gov.by). Нет структурированных
  цены/площади, дублируют gki/torgi.gov. Парсить дорого ради дублей — пока не пишем.
  **OCR-барьер СНЯТ (24.06):** `auctions_common.pdf_text(путь_или_url, force_ocr=...)` вскроет
  PDF-сканы (текстовый слой → pdftotext, скан → tesseract rus; проверено на живых данных, печатный
  русский ≈идеально, ~7с/стр). Звать ТАМ, ГДЕ НАДО — только если решим парсить эти PDF.
- ⏸️ **au.nca.by (403), gostorg.by (timeout), elot.by (timeout), cpo.by (404)** — под нашим
  ПОЛЬСКИМ IP (Psiphon) недоступны; вероятно ГЕО-блок гос-сайтов (урок kufar). Не оценены —
  проверить с белорусского IP (Psiphon OFF), тогда решить про парсер.
- Разделы существующих доменов на потом: e-auction `/gos/`, gki `auction-auinf_live`,
  beltorgi `/kvartiry/` — живые, можно расширить наши парсеры (доп. лоты), пока не сделано.

⚠ **Субагенты Claude Code в фоне НЕ могут запускать `./bin/python`** — права из
`.claude/settings.local.json` читаются при СТАРТЕ сессии и не перечитываются на лету для
субагентов. Все 4 субагента (beltorgi/torgi.gov/butb/deloocenka) сдались на стене разрешений →
парсеры написаны мной напрямую. Для будущих субагентов нужен рестарт Claude Code после правки прав.
